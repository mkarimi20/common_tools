apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dashboard-auth.fullname" . }}-test-connection"
  labels:
    {{- include "dashboard-auth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dashboard-auth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
