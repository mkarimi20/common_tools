apiVersion: v1
kind: Service
metadata:
  name: {{ include "dashboard-auth.fullname" . }}
  labels:
    {{- include "dashboard-auth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dashboard-auth.selectorLabels" . | nindent 4 }}
